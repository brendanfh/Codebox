// Generated by CoffeeScript 2.4.1
(function() {
  $(document).ready(function() {
    $('#code-editor').each(function(_, v) {
      var $v, editor;
      editor = ace.edit('code-editor');
      editor.setTheme('ace/theme/chaos');
      editor.setShowPrintMargin(false);
      editor.setFontSize(16);
      $v = $(v);
      if ($v.attr('data-lang')) {
        editor.session.setMode(`ace/mode/${$v.attr('data-lang')}`);
      }
      if ($v.attr('data-readonly')) {
        return editor.setReadOnly(true);
      }
    });
    return $('[data-setonload]').each(function(_, el) {
      var $el, change, changes, data, prop_val;
      $el = $(el);
      data = $el.attr('data-setonload');
      changes = data.split(',');
      for (change of changes) {
        change.trim();
        prop_val = (change.split(':')).map(function(x) {
          return x.trim();
        });
        $el.css(prop_val[0], prop_val[1]);
      }
    });
  });

}).call(this);

//# sourceMappingURL=main.js.map
